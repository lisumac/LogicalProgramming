class PalindromeKt {

    fun plaidrome(){
        var str ="aba"
        var charStr = str.toCharArray();
        var length = charStr.size -1;
        var  end =0
        var tempStr = ' '
//for(i = size -1, i>=0;i--)

        while (charStr.size >=length) {

            tempStr = charStr[end]
            length--



        }
        if (tempStr.equals(str)){
            println("palindrome")
        }
for (i in length downTo 0){

        }


    }


}